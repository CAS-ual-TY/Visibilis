 package de.cas_ual_ty.mundusmagicus.magic;

import javax.annotation.Nullable;

public abstract class NodeExec extends Node
{
	public NodeExec(int posX, int posY, int outputAmt, int inputAmt)
	{
		super(posX, posY, outputAmt, inputAmt);
	}
	
	/**
	 * Get the next output of the execution data type which basically just decides the next node to calculate. The next node's non calculated parents are calculated before.
	 * @return
	 */
	@Nullable
	public abstract Output getNextExec();
}
